void main () {
  int ages = 12-19;
  var confirm = ( ages <= 20) ? 'Teenage.' : 'not teenage';
  print(confirm);


  var age = 18;
  var check = (age>=18) ? 'you are a voter.' : 'you are not a voter';
  print(check);

  var selection = 2;
  var output = (selection ==0) ? 'apple' : 'banana';
  print(output);
  int num1 = 12;
  int num2 = 15;
  int max = (num1 < num2) ? num1 : num2;

  print('the greatest valve is $max');
}